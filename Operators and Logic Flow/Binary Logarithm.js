function binaryLogarithm(arr) {
    arr = arr.map(Number);

    for(let number of arr) {
        console.log(Math.log2(number))
    }
}

// function binaryLogarythm(nums) {
//   for (let x of nums) {
//     console.log(Math.log2(x));
//   }
// }